# chezmoi config — generated per machine by `chezmoi init`.
# Conservative defaults: this repo is the shared base layer, so never
# auto-commit or auto-push; commits are deliberate and manual.
[git]
	autoCommit = false
	autoPush = false
